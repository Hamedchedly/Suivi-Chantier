# 🗺️ PLAN DE ROUTE SUIVI-CHANTIER — Phase 1 & Phase 2

**Établi** : 2026-09-09  
**Statut** : En démarrage  
**Branche** : `develop`  
**Référence** : SPRINT_PHASE1.md + ARCHITECTURE.md + DATA_MODEL.md

---

## 📊 Vue d'Ensemble

### Phases de Développement

| Phase | Nom | Durée | Livrables Clés |
|-------|-----|-------|-----------------|
| **Phase 0** | ✅ Audit | 1 jour | Rapport complet, architecture cible, docs |
| **Phase 1** | 📋 Stabilisation socle | 15 jours | Migration 009, Types, Dashboard enrichi, Spike Gantt |
| **Phase 2** | 🎯 Moteur Gantt | 15 jours | Gantt interactif, dépendances, baseline, CPM |
| **Phase 3+** | 🏗️ Modules complémentaires | TBD | RFI, VISA, Finances, CR automatique |

### Priorités Absolues

1. **Migration 009** → SQL schema complet (CPM, baseline, réserves, calendriers)
2. **Gantt interactif** → Moteur temporel central
3. **Dashboard enrichi** → KPI réels (avancement, retards, budget)
4. **Connexion Planning/Terrain** → Lien observations ↔ schedule_items

---

## 🎯 PHASE 1 — Stabilisation du Socle (15 jours)

### Objectif
Obtenir un socle propre, sans dette technique cachée, prêt pour le moteur Gantt Phase 2.

### Structure (3 Sprints)

```
Sprint 1 (J1–J4)    Sprint 2 (J5–J10)      Sprint 3 (J11–J15)
Migrations + Types  UI enrichie + Hooks    Réserves + Spike Gantt
```

---

## 📅 SPRINT 1 — Migrations & Types (Jours 1-4)

### Objectif
Aligner DB et TypeScript. Aucun breaking change, seulement des ajouts.

### Jour 1 — Migration 009 (Gantt CPM Baseline)

**Fichier** : `supabase/migrations/202609090009_gantt_cpm_baseline.sql`

**Contenu** :
- ✅ Enrichir `operations` : référence_interne, moa, moe, amo, start_date, budget_global
- ✅ Enrichir `lots` : amount_contract_ht, responsible_name, lot_status
- ✅ Enrichir `schedule_items` : company_id, responsible_user_id, is_milestone, is_critical, wbs_code, priority, CPM fields (early_start, early_finish, late_start, late_finish, total_float, free_float), baseline_start/end
- ✅ Enrichir `schedule_dependencies` : lag_days
- ✅ Créer `schedule_calendars` + `schedule_calendar_exceptions` (RLS inclus)
- ✅ Créer `schedule_baselines` + `schedule_baseline_items` (RLS inclus)
- ✅ Créer `reserves` + indexes (RLS inclus)

**Validation** :
```
□ Migration appliquée localement (supabase db push)
□ 0 erreur SQL
□ RLS activé sur chaque nouvelle table
□ Triggers assert_same_operation vérifiés
```

**Status** : 🟡 À commencer

---

### Jour 2 — Types TypeScript Enrichis

**Fichier** : `src/lib/types.ts`

**Mise à jour** :
```typescript
// ScheduleItem complet
export type ScheduleItem = {
  id: string
  operation_id: string
  lot_id: string | null
  task_id: string | null
  unit_id: string | null
  parent_id: string | null
  company_id: string | null
  responsible_user_id: string | null
  title: string
  wbs_code: string | null
  planned_start: string | null
  planned_end: string | null
  planned_duration: number | null
  actual_start: string | null
  actual_end: string | null
  actual_duration: number | null
  progress: number | null
  status: ScheduleStatus | null
  priority: Priority
  is_milestone: boolean
  is_critical: boolean
  baseline_start: string | null
  baseline_end: string | null
  early_start: string | null
  early_finish: string | null
  late_start: string | null
  late_finish: string | null
  total_float: number | null
  free_float: number | null
  notes: string | null
  sort_order: number
}

export type ScheduleStatus = 'not_started' | 'in_progress' | 'done' | 'blocked' | 'postponed' | 'cancelled'
export type Priority = 'low' | 'normal' | 'high' | 'critical'
export type DependencyType = 'finish_to_start' | 'start_to_start' | 'finish_to_finish' | 'start_to_finish'

export type ScheduleDependency = {
  schedule_item_id: string
  predecessor_id: string
  dependency_type: DependencyType
  lag_days: number
}

export type ScheduleBaseline = { /* ... */ }
export type Reserve = { /* ... */ }
```

**Validation** :
```
□ tsc --noEmit → 0 erreur
□ Toutes les fonctions existantes compilent
```

**Status** : 🟡 À commencer

---

### Jour 3 — Data Layer (src/lib/data.ts)

**Nouvelles fonctions** :
```typescript
// Schedule CRUD
export async function listScheduleItems(operation_id: string): Promise<ScheduleItem[]>
export async function createScheduleItem(operation_id: string, values: ScheduleItemValues): Promise<ScheduleItem>
export async function updateScheduleItem(id: string, values: Partial<ScheduleItemValues>): Promise<void>
export async function deleteScheduleItem(id: string, operation_id: string): Promise<void>

// Dependencies
export async function listScheduleDependencies(operation_id: string): Promise<ScheduleDependency[]>
export async function upsertScheduleDependency(dep: ScheduleDependency): Promise<void>
export async function deleteScheduleDependency(item_id: string, pred_id: string): Promise<void>

// Baselines
export async function createBaseline(operation_id: string, name: string, items: ScheduleItem[]): Promise<ScheduleBaseline>
export async function listBaselines(operation_id: string): Promise<ScheduleBaseline[]>

// Reserves
export async function listReserves(operation_id: string): Promise<Reserve[]>
export async function createReserve(operation_id: string, values: ReserveValues): Promise<Reserve>
export async function updateReserve(id: string, values: Partial<ReserveValues>): Promise<void>
```

**Validation** :
```
□ Toutes les fonctions typées (pas de any)
□ Gestion erreur consistante
```

**Status** : 🟡 À commencer

---

### Jour 4 — Tests manquants

**Nouveaux fichiers** :
- `src/lib/schedule.test.ts` — Tests mapping schedule → gantt, retards, tâches critiques
- `src/lib/data.test.ts` — Tests CRUD avec mocks Supabase

**Validation** :
```
□ vitest run → tous les tests passent
□ Coverage > 60%
```

**Status** : 🟡 À commencer

---

## 📅 SPRINT 2 — UI Enrichie (Jours 5-10)

### Jour 5–6 — OperationForm & Administration Enrichis

**OperationForm** : Ajouter les champs métier
- reference_interne
- moa / moe / amo
- start_date / contractual_end_date
- budget_global
- operation_type (radio)
- status (select avec tous les statuts)

**Administration - Lot** : Enrichir
- amount_contract_ht
- responsible_name
- lot_status

**Status** : 🟡 À commencer

---

### Jour 7–8 — Dashboard Enrichi

**KPIs à ajouter** :
- Avancement global (%) = moyenne pondérée progress_entries
- Budget engagé vs budget global
- Retard global (jours)
- Nombre de tâches en retard
- Nombre d'observations ouvertes

**Blocs visuels** :
- Avancement par lot (barres %)
- Alertes (observations retard, tâches sans maj)

**Status** : 🟡 À commencer

---

### Jour 9 — Hooks Partagés

**Créer** `src/hooks/`
- `useOperation.ts` — units, lots, companies, loading/error
- `useSchedule.ts` — items, dependencies, baselines, loading/error
- `useAlerts.ts` — late tasks, open observations, counts

**Status** : 🟡 À commencer

---

### Jour 10 — Polish & Vérifications

```
□ tsc --noEmit → 0 erreur
□ eslint → 0 warning
□ vitest run → 100% passage
□ Build Vite → succès
□ Aucune fonctionnalité existante cassée
□ RLS vérifiée
```

**Status** : 🟡 À commencer

---

## 📅 SPRINT 3 — Réserves & Spike Gantt (Jours 11-15)

### Jour 11–13 — Module Réserves

**Nouveau composant** : `src/components/reserves/ReserveList.tsx`

**Fonctionnalités** :
- Liste des réserves
- Filtres (lot, unité, statut, priorité)
- Création rapide
- Changement de statut
- Numérotation auto (R-001, R-002, ...)

**Navigation** : Ajouter onglet "Réserves" dans nav principale

**Status** : 🟡 À commencer

---

### Jour 14–15 — Spike Gantt (Proof of Concept)

**Objectif** : Valider `gantt-task-react` fonctionnel avec données réelles.

**Étapes** :
```bash
npm install gantt-task-react
```

**Composant** : `src/components/gantt/GanttPanel.tsx` (lecture seule)

**Validation** :
- schedule_items s'affichent correctement
- Zoom / scroll fonctionne
- Drag & drop visuellement OK (sans persistence)
- Décision confirmée : continuer avec cette lib ou pivoter

**Status** : 🟡 À commencer

---

## ✅ Critères de Sortie Phase 1

```
□ Migration 009 appliquée et stable
□ Types TypeScript alignés sur SQL
□ tsc → 0 erreur | eslint → 0 warning | vitest → 100%
□ Build prod → succès
□ Dashboard enrichi avec KPIs réels
□ OperationForm avec tous les champs métier
□ Module Réserves opérationnel
□ Spike Gantt validé (gantt-task-react affiché)
□ Hooks useOperation / useSchedule créés
□ Aucune fonctionnalité existante cassée
□ RLS intègre sur toutes les tables
□ Docs commitées
```

---

## 🎯 PHASE 2 — Moteur Gantt (Objectif : 15 jours)

### Semaine 1 — Socle Gantt

**Tâches** :
1. Rendu de base avec gantt-task-react
2. Mapping ScheduleItem → Task
3. Drag & drop horizontal (changement date)
4. Resize vertical (changement durée)
5. Dépendances FS (natif)

**Livrables** :
- `src/components/gantt/GanttPanel.tsx` — Conteneur principal
- `src/components/gantt/GanttToolbar.tsx` — Toolbar (zoom, vues)
- `src/components/gantt/GanttSidebar.tsx` — Sidebar navigation

---

### Semaine 2 — Dépendances & Swimlanes

**Tâches** :
1. Dépendances SS/FF/SF (override)
2. Lag jours (décalage dépendances)
3. Swimlanes par lot (groupes)
4. Jalon (milestone)
5. Persistance Supabase (onDateChange, onProgressChange)

---

### Semaine 3 — CPM & Baseline

**Tâches** :
1. Moteur CPM dans `src/lib/cpm.ts`
2. Coloration chemin critique (rouge)
3. Affichage baseline (barre grisée)
4. Comparaison versions
5. Détection tâche en retard

---

## 📋 Ordre de Priorité (Si Délai Réduit)

Si Phase 1 doit être compressée à 10 jours :

1. **INDISPENSABLE** : Migration 009 + Types (J1–J2)
2. **INDISPENSABLE** : data.ts schedule (J3)
3. **INDISPENSABLE** : Spike Gantt (J4–J5)
4. **IMPORTANT** : Module Réserves (J6–J8)
5. **UTILE** : Dashboard KPIs (J9)
6. **OPTIONNEL** : Hooks, tests, polish

---

## 🚀 Démarrage Immédiat

### Jour 1 — Matin
- [ ] Lire DATA_MODEL.md section 4 (Migration 009 SQL complet)
- [ ] Créer `supabase/migrations/202609090009_gantt_cpm_baseline.sql`
- [ ] Tester migration localement

### Jour 1 — Après-midi
- [ ] Lancer `supabase db push`
- [ ] Vérifier RLS sur chaque table
- [ ] Valider 0 erreur SQL

### Jour 2
- [ ] Mettre à jour `src/lib/types.ts`
- [ ] Valider `tsc --noEmit → 0 erreur`
- [ ] Vérifier compilation tous les composants existants

---

## 📦 Stack Confirmé

| Technologie | Version | Rôle |
|---|---|---|
| React | 19.1.1 | UI Framework |
| TypeScript | ~5.9.2 | Type safety |
| Vite | 7.1.5 | Build |
| Supabase | 2.57.4 | Backend |
| Vitest | 3.2.4 | Tests |
| **gantt-task-react** | TBD | **Gantt (Phase 2)** |

### À installer (Phase 1, Jour 14)
```bash
npm install gantt-task-react
```

---

## 🔗 Références Documentation

| Doc | Utilité |
|-----|---------|
| [ARCHITECTURE.md](docs/ARCHITECTURE.md) | Structure modules + principes |
| [DATA_MODEL.md](docs/DATA_MODEL.md) | Schema SQL + migration 009 |
| [SPRINT_PHASE1.md](docs/SPRINT_PHASE1.md) | Plan jour-par-jour détaillé |
| [GANTT_LIBRARY.md](docs/GANTT_LIBRARY.md) | Comparaison librairies + décision |
| [CAHIER_DES_CHARGES.txt](docs/CAHIER_DES_CHARGES.txt) | Specs complètes (68 sections) |

---

## 📊 Tracking Avancement

Mis à jour en temps réel :

### Phase 1 Progress

**Sprint 1 (J1-J4)** : 0 / 4 jours
- [ ] J1 — Migration 009
- [ ] J2 — Types TypeScript
- [ ] J3 — Data layer
- [ ] J4 — Tests

**Sprint 2 (J5-J10)** : 0 / 6 jours
- [ ] J5-J6 — Forms enrichis
- [ ] J7-J8 — Dashboard
- [ ] J9 — Hooks
- [ ] J10 — Polish

**Sprint 3 (J11-J15)** : 0 / 5 jours
- [ ] J11-J13 — Réserves
- [ ] J14-J15 — Spike Gantt

---

## ⚠️ Règles Absolues (Ne Jamais Oublier)

1. ✅ **Ne jamais recréer une table existante**
2. ✅ **Migrations versionnées uniquement** (format YYYYMMDDNNNN)
3. ✅ **RLS activé sur chaque table nouvelle**
4. ✅ **tsc --noEmit → 0 erreur avant chaque commit**
5. ✅ **Tests passent (vitest run)**
6. ✅ **Build réussit (npm run build)**
7. ✅ **Pas de donnée métier codée en dur**
8. ✅ **Photos → Supabase Storage, jamais Git**
9. ✅ **Aucune fonctionnalité existante supprimée sans justification**
10. ✅ **RLS intègre même après ajout colonnes**

---

*Roadmap vivant — mises à jour après chaque session de développement.*

**Prochaine étape** : Démarrer Sprint 1, Jour 1 (Migration 009)

---

## 🔮 BACKLOG PRODUIT (noté le 2026-09-10)

### Dashboard Super-Admin (phase ultérieure — PAS maintenant)
Accès général transverse à **tous les projets et tous les utilisateurs** :
- Vue/gestion de l'ensemble des opérations et des comptes.
- Capacité à **forcer** des états (débloquer, réattribuer, corriger des données).
- **Matrice de droits par fonctionnalité** : cocher, pour chaque utilisateur et
  chaque projet, les modules/fonctionnalités accessibles (planning, finances,
  DPGF, RFI/visas, alertes, partage MOA, etc.).
- Prérequis : activation de l'authentification + backend Supabase (multi-projets,
  table `operation_members` + rôles, feature flags par user/projet).
- À cadrer quand le backend/auth sera enclenché.
